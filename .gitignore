node_modules/
dist/
.vite/
*.local
*.tsbuildinfo
.DS_Store

# Data snapshots are loaded at runtime, never committed. The build step
# (scripts/copy-data.mjs) populates public/data/ from the upstream pipeline.
public/data/

# Local build artifact.
morocco-cities-tool-build.zip
